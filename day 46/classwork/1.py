# Tuple-ის შექმნა 5 რიცხვით
numbers = (3, 7, 2, 7, 3)

# მინიმალური და მაქსიმალური მნიშვნელობების დაბეჭდვა
print( min(numbers))
print( max(numbers))

# პირველი ელემენტის რაოდენობის დათვლა
first_element_count = numbers.count(numbers[0])
print(first_element_count)
